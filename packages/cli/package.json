{
  "name": "agents-inc",
  "version": "0.163.0",
  "description": "CLI for managing Agents Inc. skills, stacks, and agents for Claude Code",
  "//generate": "The write path for all three generators, in dependency order: generate:types reads the skills marketplace and writes src/cli/types/generated/, then generate:schemas copies SKILL_SLUGS and CATEGORIES out of it into metadata.schema.json's enums, then generate:matrix vendors the same directory into packages/matrix. A marketplace change drifts all three, so every caller runs all three — .github/workflows/regenerate-catalog.yml spells the sequence out step by step and could call this instead. It replaces the write path only: the :check variants stay separate because CI deliberately runs different subsets of them in different jobs.",
  "//no-shared-vitest-config": "A three-project suite — unit, integration and commands, each with its own include and the retry that separates them — behind one root config that also carries vitest.setup.ts, disableConsoleIntercept for @oclif/test and ink-testing-library, and its own timeouts. @workspace/vitest-config is a single-project node config that turns globals off, which this suite needs on, so merging it in would contradict as much as it contributed. e2e/vitest.config.ts stands under the same decision. Read by scripts/check-shared-vitest-config.ts in packages/cli.",
  "//test:smoke": "The second half of e2e/vitest.config.ts, which is now two named projects over one tree: `test:e2e` runs `*.e2e.test.ts` and this runs `*.smoke.test.ts`. Before this pair existed no command ran the smoke files at all — the old single `include` excluded them, vitest has no `--include` flag, and reaching them took a hand-written config nobody had. Three files sat outside every gate for months and rotted exactly as you would expect: a marketplace fixture the Claude CLI has always rejected sat under a spec named 'should add a marketplace from a local directory source' that had never once added one. It DOES run in CI, and deliberately, even though every describe there is `skipIf(!claudeAvailable)` and the runner has no `claude`: what a fully-skipped run still proves is that the files collect, import and resolve their helpers, which is the whole of what rotted. It is not coverage of the Claude CLI on a runner and must not be reported as such — for that, run it on a machine that has the binary, where all 20 assertions execute in about ten seconds.",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    },
    "./config": {
      "import": "./dist/config-exports.js"
    }
  },
  "bin": {
    "agents-inc": "dist/index.js",
    "agentsinc": "dist/index.js"
  },
  "files": [
    "dist/",
    "assets/",
    "src/agents/",
    "src/schemas/",
    "LICENSE",
    "README.md",
    "CHANGELOG.md"
  ],
  "keywords": [
    "cli",
    "claude",
    "claude-code",
    "skills",
    "composable skills",
    "agents",
    "stacks",
    "plugins",
    "ai",
    "anthropic",
    "developer-tools",
    "productivity",
    "prompt-engineering",
    "ai-tools"
  ],
  "sideEffects": false,
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "oclif": {
    "bin": "agents-inc",
    "dirname": "agents-inc",
    "commands": {
      "strategy": "pattern",
      "target": "./dist/commands"
    },
    "plugins": [
      "@oclif/plugin-help",
      "@oclif/plugin-autocomplete",
      "@oclif/plugin-not-found",
      "@oclif/plugin-warn-if-update-available"
    ],
    "hooks": {
      "init": [
        "./dist/hooks/init"
      ]
    },
    "topicSeparator": " "
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "agentsinc": "node dist/index.js",
    "agentsinc:dev": "bun src/cli/index.ts",
    "//test": "No `pretest` hook, deliberately, and removing it fixed a real defect. turbo.json already orders `build` before `test` and `test:e2e` via dependsOn, so an npm pre-hook is a SECOND build — and turbo runs those tasks concurrently, so `turbo run test test:e2e` fired three tsup builds and two of them raced on one dist/. tsup builds with clean:true, so the loser died with `ENOENT ... unlink dist/chunk-*.js` out of pretest:e2e; measured 2026-08-23 at 3 tsup invocations per invocation, now 1. That race aborted a push with no legible error, four hundred lines up in interleaved output. Running vitest directly is unaffected and stays a refusal rather than a rebuild: assertDistIsFresh in src/cli/lib/testing/dist-staleness.ts names a stale or absent dist and says to run `bun run build` — which is the posture that file argues for, since a direct `vitest run` should stay fast and stay something you asked for. A SECOND thing the two suites share, and it is not the dist/ one: each keeps a fixture at a FIXED os.tmpdir() path — the E2E source tree, and the unit suite's checkout of the default marketplace — because globalSetup runs in vitest's own process while the specs run in forked workers, so neither side can be handed the location and both compute it. A fixed path belongs to the machine and not to the run, so the rule it costs is that a fixture is written by the runner that owns it and by nothing else. shared-source.test.ts broke that for months: collected by the `unit` project, it rebuilt and removed the E2E fixture on the real path in six specs and an afterEach, and both suites stayed green while it deleted the tree the E2E specs read. src/cli/lib/__tests__/shared-fixture-writers.test.ts holds the rule against a walk of the tree, and src/cli/lib/__tests__/helpers/shared-source.ts states it in full.",
    "test": "vitest run",
    "test:e2e": "vitest --config e2e/vitest.config.ts --run --project e2e",
    "test:smoke": "vitest --config e2e/vitest.config.ts --run --project smoke",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.scripts.json --noEmit && tsc -p e2e/tsconfig.json --noEmit && echo '✓ No TypeScript errors'",
    "typecheck:scripts": "tsc -p tsconfig.scripts.json --noEmit",
    "generate": "bun run generate:types && bun run generate:schemas && bun run generate:matrix && bun run generate:compile",
    "generate:types": "bun scripts/run-generate-source-types.ts",
    "generate:types:check": "bun scripts/run-generate-source-types.ts --check",
    "generate:schemas": "bun scripts/run-generate-json-schemas.ts",
    "generate:schemas:check": "bun scripts/run-generate-json-schemas.ts --check",
    "generate:matrix": "bun scripts/run-generate-matrix-package.ts",
    "generate:matrix:check": "bun scripts/run-generate-matrix-package.ts --check",
    "generate:compile": "bun scripts/run-generate-compile-package.ts",
    "generate:compile:check": "bun scripts/run-generate-compile-package.ts --check",
    "prepublishOnly": "bun run format:check && bun run lint && bun run typecheck && bun run generate:schemas:check && bun run generate:types:check && bun run build && bun run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/agents-inc/agents-inc.git",
    "directory": "packages/cli"
  },
  "author": "Vincent Bollaert",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/agents-inc/agents-inc/issues"
  },
  "homepage": "https://github.com/agents-inc/agents-inc#readme",
  "dependencies": {
    "@inkjs/ui": "^2.0.0",
    "@oclif/core": "^4.0.0",
    "@oclif/plugin-autocomplete": "^3.0.0",
    "@oclif/plugin-help": "^6.0.0",
    "@oclif/plugin-not-found": "^3.0.0",
    "@oclif/plugin-warn-if-update-available": "^3.0.0",
    "@oclif/table": "^0.5.0",
    "chalk": "^5.6.2",
    "execa": "^9.0.0",
    "fast-glob": "^3.3.0",
    "fs-extra": "^11.2.0",
    "giget": "^1.2.0",
    "gray-matter": "^4.0.3",
    "ink": "^7",
    "jiti": "2.7.0",
    "liquidjs": "^10.24.0",
    "react": "^19.2.6",
    "remeda": "^2.33.6",
    "yaml": "^2.9.0",
    "zod": "^4.4.3",
    "zustand": "^5.0.14"
  },
  "//devDependencies": "@workspace/matrix, @workspace/compile and @workspace/api are private and never installed by a consumer, so all three are devDependencies and tsup bundles them (see `noExternal`). matrix is the one home of the seed wire contract, which src/cli/lib/seed/ used to hand-vendor; compile is the one home of the config-pair and compiled-agent renderers, which the editor's output preview draws with; api is the one home of the typed client both front doors reach the worker through, which src/cli/lib/seed/fetch-seed.ts used to hand-write a fetch for. api pulls `hono/client` in with it, and `hono` is deliberately NOT a dependency here: tsup externalises exactly what `dependencies` names, so leaving it out is what inlines it into the bundle rather than adding a runtime install to a CLI that makes one GET. The cost of that was measured before it was accepted — `find dist -name '*.js' -not -name '*.map' | xargs du -cb | tail -1` is the number. @workspace/api-mocks and msw are a different case and are named in neither `noExternal` nor this rule: nothing under src/cli/ imports them, so they reach no bundle at all — they are the one description of the worker this suite's network mocks are resolved from, and the editor's suites read the same one.",
  "devDependencies": {
    "@eslint/js": "^10",
    "@workspace/eslint-config": "workspace:*",
    "@workspace/api": "workspace:*",
    "@workspace/api-mocks": "workspace:*",
    "@workspace/compile": "workspace:*",
    "@workspace/matrix": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "@lydell/node-pty": "^1.2.0-beta.3",
    "@oclif/test": "^4.0.0",
    "@types/fs-extra": "^11.0.0",
    "@types/node": "^24",
    "@types/react": "^19",
    "@vitest/coverage-v8": "^4.0.18",
    "@xterm/headless": "^6.0.0",
    "eslint": "^10",
    "eslint-config-prettier": "^10",
    "eslint-plugin-react-hooks": "^7.1.1",
    "ink-testing-library": "^4.0.0",
    "msw": "^2",
    "prettier": "^3.8.3",
    "tree-kill": "^1.2.2",
    "tsup": "^8.0.0",
    "typescript": "~6",
    "vitest": "^4"
  },
  "engines": {
    "node": ">=22"
  }
}
